# 更新记录

## v0.1.31 — 2026-08-07 · 新增「绝对收敛与条件收敛」课案 + 题库至 128 题

### 新增
- **`absolute-convergence` 课案（绝对收敛与条件收敛，§7.4）**：4 步全交互：
  1. 两类收敛定义 — 绝对($\\sum|a_n|$收敛) vs 条件($\\sum a_n$收敛但$\\sum|a_n|$发散)，p 滑块调 $\\sum(-1)^{n-1}/n^p$ 看绝对/条件切换
  2. 绝对收敛等价刻画 — 正负项分别拆解，N 滑块看正部和、负部和同步发散但差收敛到 ln2
  3. 判别流程 — 流程图(先判绝对值级数→收敛则绝对，发散再判交错)，ρ 滑块走流程分支
  4. 典型例题分类 — $\\sum(-1)^n/n^2$(绝对)、$\\sum(-1)^n/\\sqrt n$(条件)、$\\sum(-1)^n$(发散)三类对比
- 题库补充 4 题（定义、判别、性质判断、判别流程多选），**总题数 128**。

### 验证
- 严格校验(结构+模糊+初始+题库四层)：33 套 / 128 题 **0 问题** ✓
- 模糊测试抓出 step3 onControl 索引越界(layers[11]→[10])，已修复 ✓
- 浏览器验证：4 步全部切换成功，step3 ρ 滑块联动正常 ✓

---

## v0.1.30 — 2026-08-07 · 新增「交错级数」课案 + 题库至 124 题

### 新增
- **`alternating-series` 课案（交错级数与莱布尼茨判别法，§7.3）**：4 步全交互：
  1. 交错级数概念 — $\\sum(-1)^{n-1}a_n$ 形式，交错调和级数 $1-1/2+1/3-\\cdots=\\ln2$，N 滑块看之字形收敛
  2. 莱布尼茨判别法 — $a_n$ 递减 + 趋零两条件，p 滑块调 $a_n=1/n^p$ 看收敛/发散切换
  3. 截断误差 — $|R_n|\\le a_{n+1}$，n 滑块看误差带随通项缩窄
  4. 条件收敛与 Riemann 重排 — 重排改变和（"2正1负"重排 $\\ln2\\to\\frac32\\ln2$），N 滑块对比两条收敛曲线
- 题库补充交错级数 4 题（概念、莱布尼茨条件多选、截断误差填空、Riemann 重排），**总题数 124**。

### 验证
- 校验脚本：32 套课案 / 124 题 **0 问题** ✓
- 浏览器：首页 32 卡片、step1 公式与 N 滑块渲染正常 ✓

---

## v0.1.29 — 2026-08-07 · 新增「牛顿法」课案 + 题库至 120 题

### 新增
- **`newtons-method` 课案（方程近似解，§4.7）**：4 步，全部带 slider 交互：
  1. 迭代公式 — $x_{n+1}=x_n-f(x_n)/f'(x_n)$，以 $f(x)=x^2-2$ 求 $\\sqrt 2$ 为例，拖 $x_n$ 看切线交点逼近真根
  2. 几何直觉 — 追踪 4 次迭代序列，拖 $x_0$ 看收敛轨迹
  3. 二次收敛 — 对数刻度展示误差平方衰减，对比二分法线性收敛
  4. 陷阱与对策 — $f(x)=x^3$ 三重根处 $f'\\to 0$ 发散演示，拖 $x_n\\to 0$ 看切线变平
- 题库补充牛顿法 4 题（迭代公式、计算、收敛速度、陷阱多选），**总题数 120**。

### 验证
- 自研校验脚本：31 套课案结构 + scene 协议 + 题目字段 **0 问题** ✓
- 浏览器实跑：首页 31 张卡片、step1 公式与滑块渲染正常 ✓

### 备注
- 发现并修正架构遗漏：新课案除在 `index.js` 注册外，还需在 `index.html` 补 `<script>` 引用（项目非自动扫描）。

---

## v0.1.28 — 2026-08-07 · 30 套课案交互补全 + 题库扩至 116 题

### 课案交互完善（12 套，共补 17 个滑块步骤）
对前 30 套课案做"完整度体检"，发现 12 套课案的某些步骤缺 slider 交互（纯静态图）。本轮全部补齐，每套均达 4/4 步可交互（intro 5/5 中 4 步交互，保留 1 步总结静态）：

| 课案 | 补强内容 |
|---|---|
| `lagrange-multiplier` | step1 θ 沿圆走追踪 f 值 / step3 r 滑块联动 λ=r/2 / step4 c 滑块约束直线平移（原仅 1/4 交互，最严重）|
| `integral` | step1 n 滑块黎曼细分 / step3 加图例 / step4 n→∞ 收敛演示 |
| `lhopital` | step2 x 滑块看三次洛必达贴合 / step4 x 滑块看循环曲线同趋 1 |
| `positive-series` | step2/step4 N 滑块看部分和逼近 |
| `partial-derivative` | step3/step4 观察点滑块实时算偏导值、验证克莱罗定理 |
| `fourier-series` | step1 频率滑块 / step2 重建初始 layers / step4 锯齿波初始内容 |
| `monotonicity-extrema` | step4 区间右端点 b 滑块，看最值在端点与驻点间切换 |
| `indefinite-integral` | step4 x 滑块验证 F′=xeˣ |
| `mean-value-theorem` | step4 ξ 滑块找切线∥割线（与前三步风格统一）|
| `convexity` | step3/step4 观察点滑块、詹森不等式端点联动 |
| `double-integral` | step4 θ 滑块扫描圆域演示"先 r 后 θ" |
| `gradient` | step4 起点角度 φ 滑块，下降路径重算 |
| `ode` | step3 修正叙事与控件不匹配（R→ω，匹配相平面椭圆扁度）|

### 题库扩充（+42 题，74 → 116 题）
为 10 套原本零题的课案各补 4 题（1 基础 / 2 进阶 / 1 挑战，题型混搭 single/multi/judge/fill），并为 `ode` 补 2 题至 4 题：

`function-properties`、`inverse-composite`、`elementary-functions`、`polar-parametric`、`infinitesimals`、`cv-continuity`、`implicit-derivative`、`differential`、`monotonicity-extrema`、`curvature`（各 +4），`ode`（+2）。

现 30 套课案**全部配套 ≥3 题**，题库题型分布更均衡（新增判断/填空/多选比例上升）。

### 验证
- 自研 Node 校验脚本（加载引擎真实 `LAYER_SPECS` + 题库）：30 套课案结构 + scene 协议 + 题目字段 + courseId 关联 + onControl/bind 配对 **0 问题** ✓
- 浏览器实跑抽检：lagrange step3 r 滑块（1→1.5 联动）、integral step1 n（4→40）、fourier step2 N（3→30 初始 layers 已重建）、curvature 题库 4 题渲染作答 **全部正常** ✓
- 注：v0.1.26 CHANGELOG 记"74 题"与实际一致（v0.1.27 未新增题）；本轮起为 116 题。

### 技术说明
- 所有改动遵循现有风格：无构建、ES5 `var`/`function`、点路径 `bind`、幂用 `^`、统一调色板。
- 仅改 `js/data/courses/*.js`（12 个）、`js/data/questions.js`、`CHANGELOG.md`。**未动引擎/路由/页面逻辑**，零架构风险。
- `course.js:508` 的深拷贝对 step 级 `onControl`（在 scene 外）无影响，新增 slider 走标准路径。

---

## v0.1.27 — 2026-08-02 · 一次性新增 10 套课案，总计达 30 套

### 新增（10 套）
按北大高数教材体系补充一元微积分的全部基础章节：

| 课案 | 章节 | 主题 |
|---|---|---|
| `function-properties` | §1.2 | 有界/单调/奇偶/周期 |
| `inverse-composite` | §1.3 | 反函数与复合函数 |
| `elementary-functions` | §1.4 | 幂/指/对/三角/反三角函数族 |
| `polar-parametric` | §1.5 | 极坐标、玫瑰线、心形线、摆线、利萨如曲线 |
| `infinitesimals` | §2.5 | 无穷小比较、o/O 记号、等价无穷小替换 |
| `cv-continuity` | §2.7 | 最值定理、介值定理、一致连续 |
| `implicit-derivative` | §3.4 | 隐函数求导、参数方程求导、对数求导法 |
| `differential` | §3.5 | 微分定义、几何意义、近似计算、误差估计 |
| `monotonicity-extrema` | §4.4 | 单调性判别、极值第一/第二判别法、最值 |
| `curvature` | §4.6 | 弧微分、曲率定义、曲率公式、曲率圆 |

### 验证
- 10 套全部语法通过 + scene 协议校验通过（0 失败）
- 首页 30 张卡片，抽查 5 个新课案全部正常渲染 ✓

### 里程碑
**30 套课案**，覆盖北大高数 §1-§10（函数→极限→连续→导数→中值定理→凹凸曲率→积分→级数→多元微积分）

---

## v0.1.26 — 2026-08-02 · 新增「拉格朗日乘数法」课案（批次 3）· 题库达 74 题

### 新增
- **`lagrange-multiplier` 课案（拉格朗日乘数法，§9.6）**：批次 3 第四套。4 步：
  1. 什么是条件极值 — f=xy 在圆 x²+y²=1 上求极值，等高线与约束圆相切
  2. 拉格朗日乘数法 — ∇f=λ∇g 核心，θ 滑块沿圆移动验证梯度共线
  3. 乘数 λ 的含义 — 影子价格=df*/dc，三种约束半径展示
  4. 应用：点到直线最短距离 — f=x²+y² 约束 x+y=4，等高线与直线相切
- 联动 contour（等高线）+ parametric（约束圆/直线）。
- 题库补充拉格朗日 4 题（核心方程、几何意义、λ 含义、计算），**总题数 74**。

### 验证
- 4 步全渲染（2-2.7 万像素）、公式正常、0 真残留 ✓
- 步骤2 滑块：θ 移动时梯度共线/不共线正确切换 ✓

---

## v0.1.25 — 2026-08-02 · 新增「二重积分」课案（批次 3）· 题库达 70 题

### 新增
- **`double-integral` 课案（二重积分，§10.1-10.2）**：批次 3 第三套。4 步：
  1. 曲顶柱体的体积 — ∬f dA 概念，等高线+积分域+n滑块网格密度
  2. 累次积分（矩形域）— ∫∫=∫[∫f dy]dx 先内后外，竖条切面滑块
  3. 一般区域 X型/Y型 — 抛物线下方区域，竖条从0扫到x²/2
  4. 极坐标下二重积分 — dA=r·dr·dθ，圆形域+极坐标网格+辐射线
- 联动 contour（等高线）+ areaFill（区域填充）+ parametric（极坐标圆）。
- 题库补充二重积分 4 题（几何意义、累次积分、极坐标微元、圆面积），**总题数 70**。

### 验证
- 4 步全渲染（2.2-5 万像素）、公式正常、0 真残留 ✓
- 步骤1 滑块：n=5→10 网格点 43→128 ✓

---

## v0.1.24 — 2026-08-02 · 新增「方向导数与梯度」课案（批次 3）

### 新增
- **`gradient` 课案（方向导数与梯度，§9.5）**：批次 3 第二套。4 步：
  1. 梯度的定义 — ∇f=(∂f/∂x,∂f/∂y)，f=x²+y² 的梯度场+观察点双滑块
  2. 梯度垂直于等高线 — ∇f⊥等高线切线，角度滑块绕圆移动验证
  3. 方向导数 — D_u f=|∇f|cosθ，θ滑块改变方向，实时显示方向导数值
  4. 应用：最速下降法 — 沿-∇f走到∇f=0，下降路径折线+梯度场
- 联动 contour 等高线 + vectorField 梯度场两大原语。
- 题库补充梯度 4 题（计算、垂直性质、方向导数公式、最速下降），总题数 66。

### 验证
- 4 步全渲染（2.6-3.2 万像素）、公式正常、0 真残留 ✓
- 步骤1 双滑块：观察点变化时梯度向量与模长实时更新 ✓

---

## v0.1.23 — 2026-08-02 · 课案页侧边 AI 窗口 + 高光批注

### 新增
应「每章课件右侧自带 AI 窗口 + 高光 + 批注」需求，重构课案页为双栏布局：

- **侧边 AI 助手**：每个课案页右侧固定一个 AI 对话面板（不再跳转到独立 #/chat）。
  - 自动注入当前课案标题、步骤、内容要点作为上下文，AI 回答更贴合正在学的内容。
  - 流式渲染（SSE）+ Markdown/KaTeX，Ctrl+Enter 发送，支持清空。
  - 移动端（≤900px）自动堆叠到下方。
- **文字高光**：选中叙事区任意文字 → 弹出「🖍 高光 / ✎ 批注」菜单 → 高光保存到 localStorage。
  - 按课案 + 步骤持久化，切回该步自动显示高光列表，可删除。
- **批注旁批**：选中文字 → 批注 → 弹出编辑器写理解/疑问 → 保存显示在叙事底部。
  - 批注含原文引用 + 你的笔记，同样按课案+步骤持久化，可删除。
- 画布、控件、公式渲染等原有功能全部保留。

### 验证
- 双栏布局 + 侧边 AI 面板 + 输入框/发送/清空 全部就位 ✓
- 画布正常渲染（19684 像素）✓
- 高光列表添加/显示/持久化 ✓
- 批注添加/显示/持久化 ✓

---

## v0.1.22 — 2026-08-02 · 全局键盘快捷键体系

### 新增（交互优化）
完善全站键盘可操作性，无需鼠标即可高效导航：

| 快捷键 | 功能 |
|---|---|
| `Alt+H` | 首页 |
| `Alt+P` | 练习 |
| `Alt+E` | 考试 |
| `Alt+C` | AI 对话 |
| `Alt+S` | 设置 |
| `?` | 显示/隐藏快捷键帮助浮层 |
| `Esc` | 关闭帮助浮层 |
| `←` `→` | 课案切步骤（v0.1.4 已有） |
| `Ctrl+Enter` | 发送对话（v0.1.0 已有） |

- 输入框/textarea/select 内时快捷键不生效（避免误触）
- 顶栏右侧新增 `⌨` 按钮，点击等同 `?`
- 帮助浮层含全部快捷键说明 + kbd 键帽样式

### 验证
- `?` 弹帮助、`Esc` 关闭、`Alt+P/E/H` 导航、顶栏按钮 全部通过 ✓

---

## v0.1.21 — 2026-08-02 · 新增「偏导数」课案（批次 3 启动·多元微积分）

### 新增
- **`partial-derivative` 课案（偏导数，§9.2）**：批次 3 第一套，多元微积分起点。4 步：
  1. 多元函数与图像 — z=f(x,y) 是曲面，等高线是俯视图，x/y 双滑块移动观察点
  2. 偏导数的几何意义 — 固定 y 看切面斜率，x₀ 滑块实时显示 ∂f/∂x 值
  3. 偏导数的计算 — "其他变量当常数"口诀，马鞍面 f=x²-y² 等高线展示方向性
  4. 高阶偏导与混合偏导 — 克莱罗定理 ∂²f/∂x∂y=∂²f/∂y∂x（连续条件下）
- 充分利用 v0.1.20 的 **contour 等高线原语**画 f(x,y) 的地形图（同心圆/双曲线/四瓣花）。
- 题库补充偏导 4 题（计算、几何意义、克莱罗条件、链式），总题数 62。

### 验证
- 4 步全渲染（3.2-5.4 万像素，contour 像素丰富）、公式正常、0 真残留 ✓
- 步骤1 双滑块联动：观察点 x 变化时高度 3.25→5.00 ✓

### 大纲进度
✅ 已完成 17 套课案、62 题；批次 3 进行中（partial-derivative ✓）

---

## v0.1.20 — 2026-08-02 · 新增等高线 contour 原语（多元微积分基础设施）

### 新增
- **`contour` 原语**：绘制二元函数 $f(x,y)$ 的等高线（水平集 $\{(x,y):f=c\}$）。
  - 用 **marching squares** 算法逐网格单元求等高线段，处理全部 16 种符号组合（含鞍点双段）。
  - 字段：`fn`（双变量表达式）、`levels`（高度值数组）、`nx/ny`（网格密度）、`color/lineWidth/opacity`。
  - 复用 vectorField 的 `compileFieldFn` 双变量安全编译（字符白名单 + new Function）。
  - 这是**多元微积分课案（偏导/梯度/二重积分/拉格朗日）的关键基础设施**——
    一元课案用 plot 画曲线，多元课案用 contour 画地形图。
- 同步更新 protocol.js LAYER_SPECS（fn 必填）与 viz-protocol.md 文档。

### 验证
- f=x²+y² 的等高线（同心圆）：27356 像素正确绘制 ✓
- protocol 校验：合法通过、缺 fn 致命错误 ✓

---

## v0.1.19 — 2026-08-02 · 新增「傅里叶级数」课案，批次 2 完成

### 新增
- **`fourier-series` 课案（傅里叶级数，§8.4）**：批次 2 收尾，3b1b 经典题材。4 步：
  1. 周期函数与三角基 — sin(nx) 不同频率的"乐高积木"，正交性提取系数
  2. 方波的傅里叶逼近 — 奇次谐波 sin x+sin3x/3+... 叠加，N 滑块从正弦波→方波
  3. 频谱与吉布斯现象 — 跳变处 9% 过冲不随项数消失（只变窄），放大跳变点演示
  4. 收敛条件（狄利克雷）— 间断点收敛到左右极限平均，傅里叶 vs 泰勒对比表
- 用 400 个采样点动态画合成波形，N 滑块实时改变每个点的 y 值。
- 题库补充傅里叶 4 题（三角基、方波奇次谐波、吉布斯现象、狄利克雷间断值），总题数 58。

### 验证
- 4 步全渲染（3.3-5.8 万像素）、公式正常、0 真残留 ✓
- 步骤2 滑块：N=3→30 中间采样点 y 从 -0.04→-0.40（波形改变）✓

### 🎯 批次 2 完成
批次 2（一元进阶）5 套课案全部完成：
lhopital / convexity / integral-applications / positive-series / fourier-series
下一步进入批次 3（多元微积分：偏导/梯度/二重积分等）。

---

## v0.1.18 — 2026-08-02 · 新增「正项级数审敛法」课案（批次 2）

### 新增
- **`positive-series` 课案（正项级数审敛法，§7.2）**：批次 2 第四套。4 步：
  1. p 级数与比较判别法 — p>1 收敛，p≤1 发散，三个 p 值部分和对比
  2. 比较判别法极限形式 — lim aₙ/bₙ=c∈(0,∞) 同敛散，Σ1/(2n²-1) 实例
  3. 比值判别法（达朗贝尔）— ρ<1 收敛，ρ>1 发散，ρ 滑块演示几何级数收敛/发散切换
  4. 根值判别法（柯西）— ⁿ√aₙ→ρ，含 n 次幂通项一步到位
- 题库补充正项级数 4 题（p 级数、比值法、根值法、比较极限形式），总题数 54。
- 总结三把尺子选择顺序（比较/比值/根值/失效）。

### 验证
- 4 步全渲染（2.5-4.2 万像素）、公式正常、0 真残留 ✓
- 步骤3 滑块：ρ=0.5「收敛 S=2」→ρ=1.2「发散」切换正确 ✓

### 大纲进度
✅ 已完成 15 套课案、54 题；批次 2 仅剩 fourier-series（傅里叶级数）

---

## v0.1.17 — 2026-08-02 · 课案完成进度追踪系统

### 新增（交互优化）
给学习过程加上"成就感"——完整追踪每套课案的访问步骤，首页与课案页可视化进度：

- **进度记录**：`_writeProgress` 新增 `visited` 数组，记录用户访问过的步骤索引（去重持久化）。
- **首页进度总览**：hero 区显示「学习进度：已完成 X / N 套课案」。
- **卡片完成状态**：
  - 未访问：「N 步 · 进入 →」（灰）
  - 学习中：「学习中 2/4」（灰）
  - 已完成：「✓ 已完成」（绿）+ 卡片绿色描边 + 封面右上角绿色 ✓ 角标
- **步骤按钮标记**：已访问过的步骤按钮显示绿色 ✓，当前步骤为 primary 蓝色。
- **完成提示**：访问到最后一步且全部步骤都看过时，步骤标题显示「🎉 已完成全部步骤」。

### 验证
- 初始 0 完成 → 学完 integral 4 步 → 首页「已完成 1/14」+ 卡片「✓ 已完成」+ 绿色角标 ✓
- visited 步骤按钮 3 个（最后一步是 primary）✓
- 最后一步标题「🎉 已完成全部步骤」✓

---

## v0.1.16 — 2026-08-02 · 新增「定积分的应用」课案（批次 2）· 题库达 50 题

### 新增
- **`integral-applications` 课案（定积分的应用，§6.5）**：批次 2 第三套。4 步：
  1. 平面图形面积 — ∫[f-g]dx，sin(x) 在 [0,b] 面积填充，b 滑块累积
  2. 旋转体体积（圆盘法）— V=π∫f²dx，√x 绕 x 轴，parametric 画截面圆
  3. 旋转体体积（圆柱壳法）— V=2π∫x·f dx，绕 y 轴，壳的竖条示意
  4. 曲线弧长 — L=∫√(1+f'²)dx，弧长 vs 弦长对比
- 充分利用 areaFill（面积/体积填充）、parametric（截面圆）原语。
- 题库补充积分应用 4 题（面积意义、圆盘法体积、弧长、壳法等价），**总题数达 50**。

### 验证
- 4 步全渲染（3.7-14 万像素，areaFill 填充区像素量大）、公式正常、0 真残留 ✓
- 步骤1 滑块：b=2→3.1 时面积 1.416→1.999（逼近 π 处的理论值 2）✓

### 大纲进度
✅ 已完成 14 套课案、50 题；批次 2 剩 positive-series / fourier-series

---

## v0.1.15 — 2026-08-02 · 新增「凹凸与拐点」课案（批次 2）

### 新增
- **`convexity` 课案（函数的凹凸与拐点，§4.5）**：批次 2 第二套。4 步：
  1. 凹凸的几何直觉 — 切线在曲线上/下方，笑脸/哭脸类比，x³ 的 x 滑块演示凹凸切换
  2. 二阶导判别法 — f″>0 凹、f″<0 凸，x⁴-2x² 与其 f″=12x²-4 对照
  3. 拐点 — 凹凸反转点，x³ 有拐点 vs x⁴ 无拐点（f″=0 不变号）的对比
  4. 应用 — 极值第二判别法、詹森不等式（凹函数在割线下方）、凸优化直觉
- 题库补充凹凸 4 题（判别、拐点判定、f″=0 非充分、极值第二判别法），总题数 46。

### 验证
- 4 步全渲染（2.8-4 万像素）、公式正常（每步 7-26 个 .katex）、0 真残留命令 ✓
- 步骤1 滑块联动：x=1 显示「凹」、x=-1 显示「凸」✓

### 大纲进度
✅ 已完成 13 套课案、46 题；批次 2 剩 integral-applications / positive-series / fourier-series

---

## v0.1.14 — 2026-08-02 · 课案画布支持 PNG 截图导出

### 新增（交互优化）
- 所有课案页控件栏新增「📷 PNG」按钮，一键把当前画布导出为高清 PNG。
  - 利用 canvas 已有的高 DPR 后缓冲（设备像素），导出的图天然是 2× 分辨率（视网膜屏清晰）。
  - 文件名自动含课案 id + 步骤号 + 步骤标题，如 `integral_2_黎曼和的细分.png`。
  - 每个步骤都有此按钮，方便保存任意时刻的可视化状态用于笔记/分享。
- 实现：`course.js` 新增 `_downloadCanvas()` 方法，用 `canvas.toDataURL` + 临时 `<a download>` 触发下载。

### 验证
- 截图按钮在 limit/derivative/taylor/ode 4 个课案均存在 ✓
- 点击不报错，`toDataURL` 生成有效 `data:image/png;base64,...` ✓
- DPR=2 下导出高清图 ✓

---

## v0.1.13 — 2026-08-02 · 新增「洛必达法则」课案（批次 2 启动）

### 新增
- **`lhopital` 课案（洛必达法则，§4.2）**：批次 2 第一套。4 步：
  1. 什么是未定式 — 0/0 与 ∞/∞，直接代入失效，比值曲线逼近极限线
  2. 0/0 型 — lim f/g = lim f'/g'，eˣ 系列两次洛必达演示"曲线逐次平坦"
  3. ∞/∞ 型 — ln x / x → 0，增长速度阶层（ln≪xᵃ≪bˣ≪x!）
  4. 多次应用与陷阱 — 忘验未定式、循环（eˣ±e⁻ˣ 互为倒数）、导数比无极限≠原无极限
- 题库补充洛必达 4 题（0/0 多次、∞/∞、陷阱判断、使用条件），总题数 42。

### 验证
- 4 步全渲染（3.8-4.3 万像素）、公式正常（每步 11-22 个 .katex）、0 真残留命令 ✓
- 考试综合卷含 lhopital ✓

### 大纲进度
✅ 已完成 12 套课案、42 题；批次 2 进行中（lhopital ✓，剩 convexity / integral-applications / positive-series / fourier-series）

---

## v0.1.12 — 2026-08-02 · 新增「不定积分」课案，批次 1 完成

### 新增
- **`indefinite-integral` 课案（不定积分，§5.1-5.3）**：批次 1 收尾。4 步：
  1. 原函数与不定积分 — ∫f dx = F + C，一族平移曲线，C 滑块平移主曲线
  2. 基本积分表 — 幂/指/三角的直接结果，sin/cos 验证 F'=f（切线斜率=高度）
  3. 换元积分法（凑微分） — ∫2x·cos(x²)dx 令 u=x²，三曲线验证
  4. 分部积分法 — ∫xeˣdx 的 LIATE 选 u，结果 (x-1)eˣ 求导验证
- 题库补充不定积分 4 题（+C、基本表、换元、分部），总题数 38。
- 修复题库两处单引号转义问题（`'` 在字符串内的语法错误）。

### 验证
- 4 步全渲染（3.5-4.3 万像素）、公式正常（每步 11-21 个 .katex）、0 残留命令 ✓
- 步骤1 滑块联动：C=0→2 主曲线 fn 正确更新 ✓

### 🎯 批次 1 完成
批次 1（补全一元微积分主线）5 套课案全部完成：
limit-laws / derivative-rules / mean-value-theorem / series-basics / indefinite-integral
下一步进入批次 2（一元进阶：lhopital / convexity / integral-applications / positive-series / fourier-series）。

---

## v0.1.11 — 2026-08-02 · 新增「微分中值定理」课案（批次1）

### 新增
- **`mean-value-theorem` 课案（微分中值定理，§4.1）**：批次 1 第四套。4 步：
  1. 罗尔定理 — f(a)=f(b) 时存在 ξ 使 f'(ξ)=0，sin(x) 在 [0,π] 波峰水平切线，ξ 滑块验证
  2. 拉格朗日中值定理 — 割线斜率=某点切线斜率，x² 在 [1,3] 上 ξ=2 时切线∥割线
  3. 柯西中值定理 — 参数化视角，y=x³ 上 ξ≈1.32 时切线∥割线
  4. 应用 — 证方程有根、证 |Δsin|≤|Δx| 不等式、证 arcsin+arccos=π/2 恒等式
- 题库补充中值定理 4 题（罗尔条件、求 ξ、不等式证明、柯西退化），总题数 34。
- 考试综合卷配比更新。

### 验证
- 4 步全渲染（3.4-4 万像素）、公式正常（每步 14-20 个 .katex）、0 残留命令 ✓
- 步骤2 滑块联动：ξ 改变时切线与提示文字正确更新 ✓

### 大纲进度
✅ 已完成 10 套课案、34 题；批次 1 仅剩 indefinite-integral（不定积分）

---

## v0.1.10 — 2026-08-02 · 考试可按课案单卷 + 新增「极限运算法则」课案

### 改进
- **考试对应课案**：考试页新增「试卷类型」下拉，可选择「综合卷」或任一已学课案的「单章卷」。
  - 综合卷：按章节配比组卷（约 15-18 题，难度混合）
  - 单章卷：抽取指定课案的全部题目（如「极限与连续」5 题）
  - 考试头部显示当前试卷标题（综合卷 / 课案名），成绩记录也存标题便于回顾

### 新增
- **`limit-laws` 课案（极限的运算法则，§2.3）**：批次 1 第三套。4 步：
  1. 极限的四则运算 — 和差积商的极限=极限的和差积商（前提：各自极限存在），演示 $\infty-\infty$ 陷阱
  2. 夹逼准则 — $x^2\sin(1/x)$ 被 $\pm x^2$ 夹向 0，高频采样显示震荡衰减
  3. 等价无穷小 — sin/tan/ln/exp 四曲线在原点重合，强调替换仅限乘除因子
  4. 复合函数极限 — $e^{\sin x}$ 由内向外求极限，连续性是关键
- 题库补充 limit-laws 4 题（夹逼、等价无穷小、复合极限、替换限制），总题数 30。

### 验证
- limit-laws 4 步全渲染（3.2-4.6 万像素）、公式正常（每步 12-23 个 .katex）、0 残留 ✓
- 考试选卷下拉含全部 9 个课案 + 综合卷，limit-laws 显示「极限的运算法则（4 题）」✓
- 综合卷组卷 18 题 ✓

### 大纲进度
✅ 已完成 9 套课案、30 题；批次 1 剩 mean-value-theorem / indefinite-integral

---

## v0.1.9 — 2026-08-02 · 新增练习题库与模拟考试系统

### 新增
应「加入课程练习题库和模拟高数考试」需求，搭建完整的练习+考试子系统：

- **题库数据层** `js/data/questions.js`：26 题，覆盖现有 8 套课案的核心知识点，
  四种题型（单选/多选/判断/填空），三档难度（基础/进阶/挑战），
  每题含题干（KaTeX 公式）、选项、答案、解析、关联知识点。
  提供 `QuestionBank` 查询接口（按课案/难度筛选、按章节配比随机组卷）。
- **练习页** `#/practice`：按章节/难度筛选 → 即时判分（答对绿/答错红+标正确项）
  → 展开 KaTeX 解析 → **错题本**自动收集（localStorage，答对即移除，可重做）。
- **模拟考试页** `#/exam`：按章节配比自动组卷（约 16 题，难度混合）→
  限时倒计时（10/15/20 分钟可选，超时自动交卷）→ 交卷判分给出百分制成绩与等级
  → 逐题解析回顾（显示你的答案 vs 正确答案）。成绩记录保留最近 5 次。
- 顶栏新增「练习」「考试」入口，首页加「练习与考试」分区入口并显示题库总数。

### 验证
- 题库加载：26 题、按课案分布合理（intro×3 / limit×5 / derivative×3 / derivative-rules×4 / integral×3 / taylor×3 / series-basics×3 / ode×2）、组卷接口正常 ✓
- 练习页：26 题含公式渲染、答对判定+标绿+清错题本、答错判定+收集错题、错题本切换 ✓
- 考试页：组卷 16 题、计时显示、作答交互正常 ✓

---

## v0.1.8 — 2026-08-02 · 新增「常数项级数」课案（批次1）

### 新增
- **`series-basics` 课案（常数项级数，§7.1）**：批次 1 第二套。4 步：
  1. 级数 = 无穷个数的和 — 部分和序列 S_n，收敛 ⟺ S_n 收敛，把"无穷求和"翻译成数列极限
  2. 等比级数 — Σr^n = 1/(1-r)，r 滑块（-0.95~0.95）演示收敛/震荡收敛/收敛变慢
  3. 调和级数发散 — a_n→0 但 Σ1/n 发散的经典反例，附 ln(N) 参照曲线
  4. 级数的基本性质 — 线性性、增减有限项不变敛散、a_n→0 是必要非充分条件
- 用 plot+point 画离散部分和点序列，onControl 动态重建点集（不预生成全部，按 N 滑块截取）。

### 验证
- 4 步全渲染（2.5-3 万像素）、公式正常（每步 12-22 个 .katex）、0 残留命令 ✓
- 步骤2 滑块联动：r=0.5→0.9 时末点部分和 2.0→7.18（逼近理论极限 10）✓

### 大纲进度
✅ 已完成 8 套；批次 1 剩 limit-laws / mean-value-theorem / indefinite-integral

---

## v0.1.7 — 2026-08-02 · 建立北大体系课程大纲 + 新增「求导法则」课案

### 新增
- **`CURRICULUM.md` 课程大纲**：参照北京大学《高等数学》/《数学分析》体系，覆盖函数极限 → 一元微积分 → 多元微积分 → 级数与微分方程 → 曲面论初步，共 13 章、约 50 节，标注每节对应课案 id 与状态（✅/🚧）。
- **`derivative-rules` 课案（求导法则，§3.2）**：批次 1 首套。4 步：
  1. 四则运算法则 — f=x²/g=sin x/h=f+g 三曲线叠加切线，验证和的导数=导数之和
  2. 链式法则 — 复合 sin(x²)，"变化率接力放大"直觉
  3. 反函数求导 — eˣ 与 ln x 关于 y=x 对称，切线斜率互为倒数
  4. 三角函数族 — sin→cos→-sin→-cos 循环链，k 滑块切换曲线与颜色

### 验证
- 4 步全渲染（3.5-4.5 万像素）、公式正常（每步 15-20 个 .katex）、0 残留命令 ✓
- 步骤4 滑块联动：k=0→2 切换 sin→-sin、颜色蓝→紫 ✓

### 大纲进度
✅ 已完成 7 套（intro / limit / derivative / derivative-rules / integral / taylor / ode）
🚧 计划中约 43 套，按 5 批次推进（详见 CURRICULUM.md）

---

## v0.1.6 — 2026-08-02 · 新增零基础入门课案「微积分启程」

### 新增
应「让新大一能零基础」需求，新增第 0 课 `intro`（微积分启程），置于首页首位并加「推荐起点」徽章。
5 步循序渐进，不假设任何高中以上数学基础，最后平滑过渡到现有课案：

1. **变化的世界** — 生活例子（车速/电量/身高）引出"研究变化"的需求，建立动机
2. **函数：描述世界的语言** — 用"输入→输出机器"的类比补函数概念，x 滑块驱动 $f(x)=x^2$
3. **坐标系与图形** — 建立"看图说话"直觉（曲线陡=变化快），观察点滑块在 sin 波峰附近曲线水平
4. **瞬时变化之谜** — 核心矛盾"0÷0"，割线→切线演示，h 滑块让割线逼近切线
5. **通往微积分** — 预告四大主题（极限/导数/积分/泰勒），用 parametric 画交汇曲线地图

叙事刻意口语化、多用生活类比、公式克制（步骤1/5 几乎纯文字）。
首页给 intro 卡片加橙色「推荐起点」徽章 + 高亮描边，引导新生优先点入。

### 验证
- 首页 6 卡片，intro 在首位，含徽章 ✓
- 5 步全部渲染（画布 3.4-4.7 万像素），含公式的步骤 KaTeX 正常、0 残留命令 ✓
- 步骤4 滑块联动：h 改变后割线终点 y 9→6.25 ✓

---

## v0.1.5 — 2026-08-02 · 新增参数曲线 parametric 原语

### 新增
- **`parametric` 原语**：绘制参数曲线 $(x(t), y(t))$，`fx`/`fy` 为关于参数 `t` 的表达式。
  填补了 `plot` 只能画 $y=f(x)$、无法画闭合曲线的空白。
  用于圆、椭圆、利萨如曲线、摆线等参数化轨迹。
- 同步更新 `protocol.js` LAYER_SPECS（fx/fy 必填，缺则致命）与 `viz-protocol.md` 文档。

### 验证
- 圆 `fx=2cos(t), fy=2sin(t)`：14755 像素 ✓
- 椭圆 `fx=2.5cos(t), fy=1.5sin(t)`：14821 像素 ✓
- 利萨如曲线 3:2 `fx=2cos(3t), fy=2sin(2t)`：13930 像素 ✓
- protocol 校验：合法通过、缺 fx 致命错误 ✓

---

## v0.1.4 — 2026-08-02 · 课案进度记忆与键盘导航

### 新增
优化交互体验：课案页离开后自动记忆，下次回到同一课案会恢复到上次的步骤与控件值。

- **进度持久化**：localStorage 键 `mathviz.course.progress`，按课案 id 存 `{ step, controls }`。
  切步骤、拖滑块、离开页面时自动写入；mount 时读取并恢复。
- **键盘导航**：课案页内 ← / → 切换步骤（自动跳过输入框聚焦时不拦截）。
- 步骤条下方加「提示：← → 键切换步骤；进度自动保存」说明文字。

### 验证
- 进入 integral 步骤2，拖 n 滑块到 100 → 离开去首页 → 回到 integral，正确恢复步骤2 且 n=100 ✓
- 键盘 → 从步骤3 到步骤4、← 回到步骤3 ✓

---

## v0.1.3 — 2026-08-02 · 新增课案「微分方程与方向场」

### 新增
新增第 5 套预设课案 `ode`，充分利用 v0.1.1 的向量场原语，4 个步骤：
1. **指数增长与衰减** — `dy/dx = k·y` 方向场 + 解曲线 `y=e^(kx)`，k 滑块（-0.8~0.8）切换增长/衰减
2. **逻辑斯谛增长** — `dy/dx = k·y·(1-y/L)` S 型曲线，L 滑块控制环境容量天花板
3. **简谐振动** — 二阶方程拆成一阶组，在 (x,v) 相平面画旋转方向场 + 椭圆轨道，R 滑块控制振幅
4. **非线性系统：极限环** — van der Pol 振子 `μ(1-x²)v-x`，μ 滑块控制非线性强度

每步含方向场 + 解曲线叠加 + 滑块联动（onControl 同步更新 dy/fn）。

### 验证
- 首页 5 张卡片（含 ode）✓
- 4 步全渲染：画布 3.6-3.9 万像素、KaTeX 公式正常、0 残留命令 ✓
- 滑块联动：k 改变后方向场 `dy` 与解曲线 `fn` 正确同步更新 ✓

---

## v0.1.2 — 2026-08-02 · 修复课案公式渲染（引用块内 ε-δ 定义等）

### 修复
修复用户反馈的「课案页叙事区公式显示为原始 LaTeX 代码」问题。根因是公式渲染流程的职责耦合与时序缺陷：

- **markdown.js**：`MVmd.render` 原本在末尾立即调 `renderInHtml` 把公式固化进 HTML 字符串。
  若此刻 KaTeX 未就绪（in-app browser 资源加载时序不同），公式被永久保留为原文，后续无法补救。
  → 改为只做 Markdown，公式渲染交给调用方在 innerHTML 设置后统一处理。
- **markdown.js `inline()`**：原版先 `escapeHtml` 整段文本，会把公式内的 `>` `<` 转成 `&gt;` `&lt;`，
  KaTeX 收到实体后渲染失败。→ 改为「先保护 `$...$` 公式占位 → escape 非公式部分 → 还原公式」，
  公式内的特殊字符由 KaTeX 自行处理。
- **math.js `renderInHtml`**：重写为同样的「保护公式 → escape 非公式 → 还原渲染」策略，
  避免 `doRender` 对 textContent 二次 escape 导致 `&gt;` 进入 KaTeX。
- **math.js `renderMath`**：增加 `waitForKatex` 机制——KaTeX 未就绪时不再直接放弃，
  而是轮询等待（最多 10s）就绪后自动补渲染，根治早加载/慢网络场景。

### 验证
- 16/16 步骤（4 课案 × 4 步）全部公式正确渲染，0 残留 LaTeX 命令
- 之前漏渲染的 limit 步骤2 引用块内 ε-δ 定义（`$\varepsilon > 0$` 等）现已正常
- limit 步骤1 .katex 数 13→16、步骤2 21→25（更多公式被正确渲染）
- chat 页流式对话、home 页公式无回归

---

## v0.1.1 — 2026-08-02 · 新增向量场与面积填充原语

### 新增
- **`vectorField` 原语**：给定 `dx`/`dy` 双变量 (x,y) 表达式，在网格点画归一化箭头。
  用于展示微分方程方向场、梯度场。例如旋转场 `dx:"-y", dy:"x"`。
  支持安全字符白名单校验，含数学函数与常量注入。
- **`areaFill` 原语**：曲线与 x 轴之间的连续面积填充（比 riemann 矩形更适合展示"真实面积"），
  引擎同时返回梯形法数值积分结果。
- 同步更新 `protocol.js` 的 LAYER_SPECS（两新类型纳入校验）与 `viz-protocol.md` 文档。

### 验证
- 向量场旋转场（dx=-y, dy=x）：紫色箭头 5874 像素，14×10 网格采样正确 ✓
- 面积填充 sin(x) 在 [0,π]：绿色填充 22278 像素 + 蓝色曲线 ✓
- protocol 校验：两类型合法通过、areaFill 缺 fn 致命错误 ✓

---

## v0.1.0 — 2026-08-02 · MVP 发布

首个可用版本：3b1b 式高等数学可视化教学站 + DeepSeek 实时对话驱动可视化。

### 新增

**项目骨架**
- 独立纯前端项目（HTML/CSS/JS，零构建步骤），极简静态服务器 `server.js`（端口 8100）
- KaTeX vendored 本地副本（v0.16.11）渲染数学公式，支持 `$...$` / `$$...$$` / `\(...\)` / `\[...\]`
- hash 路由（`#/home` / `#/course/<id>` / `#/chat` / `#/settings`），页面 mount/unmount 机制

**可视化引擎**（`js/viz/`）
- `Stage`：Canvas 舞台，高 DPR 适配（视网膜屏清晰）、ResizeObserver 自适应、rAF 主循环
- `axes`：笛卡尔坐标系（网格、刻度、标签、箭头、自适应步长）
- 原语集：`plot`（函数曲线，支持 sin/cos/exp/ln/`x^2` 等）/ `point` / `tangent`（数值求导切线）/ `riemann`（左/右/中点黎曼矩形）/ `taylor`（数值求各阶导的泰勒多项式）/ `line` / `text` / `clear`
- `Timeline`：关键帧动画（play/pause/seek/reset，缓动函数，后台标签自动暂停）
- `engine.js` 统一门面 `window.VIZ`：mount/execute/clear/play/pause/seek，未知 layer 类型容错跳过
- **可视化指令协议 v1**（`viz-protocol.md`）：JSON Schema + `protocol.js` 校验器（字段级错误明细、致命/非致命分级）

**预设课案**（4 套，3b1b 经典题材）
1. **极限与连续**：数列极限 `(1+1/n)^n→e`、ε-δ 双带可视化、连续 vs 间断对比、`sin(x)/x` 重要极限
2. **导数与微分**：割线→切线动画、瞬时变化率、高阶导数（sin→cos→-sin→-cos 旋转链）
3. **积分与黎曼和**：n 滑块（1→200）实时逼近、左/右/中点取样对比、牛顿-莱布尼茨公式
4. **泰勒级数**：阶数递增逐项吞噬原函数、收敛半径直觉（ln(1+x) 的 R=1）、不同展开点

每套含 4 步分步叙事（Markdown+KaTeX）、内嵌画布、参数滑块双向绑定（onControl/bind 两种模式）。

**DeepSeek 对话层**（`js/ai/`）
- 设置页：baseURL/apiKey/model/thinkingMode/reasoningEffort 五字段自填，连通性测试（10s 超时、401/403/404/429/5xx 分类反馈），"仅本会话"模式
- 流式对话核心：SSE 解析、思维链 `reasoning_content` 与正文 `content` 分离渲染（折叠区）、system prompt 注入（含指令协议说明 + 少样本示例）
- **可视化指令派发桥** `MVbridge`：流中实时识别 ` ```viz ` 围栏、闭合时增量解析 JSON、校验后 `VIZ.execute`，非法围栏不阻塞对话
- 对话历史 localStorage 持久化：多会话（新建/切换/重命名/删除/清空）、自动标题、历史指令"应用到画布"重放
- 错误分类：auth/rate/server/network/timeout/cancel，每种给可操作提示

**安全表达式求值器**（`js/lib/math-eval.js`）
- 词法 → 中缀 → RPN → 闭包编译，不使用 eval 直接执行用户串（AI 产出可信）
- 支持变量 x/t、12+ 数学函数、常量 pi/e、运算符 `+ - * / ^ %`

### 验证
- 18 个任务全部通过自动化验证（CDP + headless Chrome）
- 端到端冒烟：20 个模块加载、8 次路由切换无残留、移动端 390px 响应式
- 离线验证：bridge 围栏识别/派发、config 读写、history 会话管理、protocol 校验器全场景
- 浏览器实测：DPR 2 倍清晰、动画 progress 精确、滑块双向绑定、四套课案 16 步全通过协议校验

### 已知限制
- DeepSeek 真实 API 联调需用户在设置页填入自己的 API Key（代码已就绪，待实测）
- API Key 存 localStorage 会暴露给同源 JS（README 已警示，提供"仅本会话"选项）
- 泰勒多项式用数值求各阶导，阶数 > 8 时精度下降
- 移动端对话页双区切换为单列堆叠（未做浮层自动切换，功能可用但体验待优化）

### 技术决策
- **独立项目**：不沿用同工作树 MindSecGo 的任何约束/品牌/组件
- **零 CDN**：KaTeX vendored 本地化，纯前端可离线运行（除 DeepSeek 调用）
- **文本→图形桥**：用 JSON 指令协议而非让前端解析自然语言，AI 输出结构化、前端校验执行
- **DeepSeek API**：默认模型 `deepseek-v4-flash`（V4-Flash-0731），思考模式 `thinking:{type:"enabled",reasoning_effort:"max"}`
